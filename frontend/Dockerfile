# Imagen base
FROM nginx:alpine

# Copiar archivos del frontend al servidor web
COPY public /usr/share/nginx/html

# Exponer el puerto 80
EXPOSE 80

# El contenedor arranca con Nginx automáticamente
